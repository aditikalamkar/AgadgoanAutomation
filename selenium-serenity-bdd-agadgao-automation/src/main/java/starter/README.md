Runner (Starting Point) --> Feature File (Gherking steps & sccenarios) --> Step Defination (Gherkin step implementation) --> Step Implementation (Business logic) (extends) --> Page Element (GUI elements) --> Reusable library (extends) --> base class [Will iuse multiple interfaces)### Application code
 
These packages generally contain application code. 
If you are writing a reusable test library, you can also place reusable test components such as Page Objects or Tasks here.